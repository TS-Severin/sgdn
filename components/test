<Carousel

    /*
    disableSwipeOnMobile
    disableDrag
    */
    responsive={responsive}
    ssr

    minimumTouchDrag={80}
    slidesToSlide={1}
    infinite={true}
    partialVisbile={true}
    centerMode={false}
    containerClass="container-with-dots"
    itemClass=""

    rewind={true}


>


    {
        images.map((image, index) => (
            <div className="flex max-h-[280px]"><img key={index} src={image.url} alt={`Foto ${index + 1}`} className="object-contain mb-8 pl-0" /></div>
        ))
    }





</Carousel > 